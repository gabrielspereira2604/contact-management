name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '6.0.x'

      - name: Restore
        run: dotnet restore contact-management.sln

      - name: Build
        run: dotnet build contact-management.sln --no-restore

      - name: Test
        run: dotnet test ContactManagement.Tests/ContactManagement.Tests.csproj --no-build --verbosity normal
